.user-new-hosting
  %div.group
    %a.home.btn.btn-info.btn-sm{:href => user_url(current_user)} « Back
    =link_to("Sign Out", signout_url, method: :delete, class: "signout")
  %header
    %p.lead Host

  = render 'hosting_form'
  = link_to "Delete", hosting_path(@hosting), method: "DELETE", class: "cancel"
