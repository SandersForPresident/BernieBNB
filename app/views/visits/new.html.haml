.user-new-visit
  %div.group
    =link_to("« Back", user_url(current_user), class: 'btn btn-info btn-sm left')
    =link_to("Sign Out", signout_url, method: :delete, class: "btn btn-info btn-sm right")
  %header
    %p.lead Travel

  = render 'visit_form'
  = link_to "Cancel", user_url(current_user), class: 'btn btn-danger btn-block btn-lg'
