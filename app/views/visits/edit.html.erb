<div class="user-new-visit">
  <a class="home btn btn-info btn-sm" href="<%= user_url(current_user) %>"> << Back </a>
  <header>
    <h1>Travel</h1>
  </header>

  <%= render "visit_form" %>

  <%= button_to "Delete", visit_path(@visit), method: "delete" %>
</div>
